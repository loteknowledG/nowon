import React from 'react';

export default function SvelTuiDemo(): JSX.Element {
  return (
    <div style={{padding:12}}>
      <h3>SvelTUI — experimental mock</h3>
      <p>Client-side mock of SvelTUI reactive patterns (keyboard + focus behavior testing).</p>
      <small>Focus nav and reactive updates can be prototyped here.</small>
    </div>
  );
}
