import React from 'react';

export default function ChalkDemo(): JSX.Element {
  return (
    <div style={{padding:12}}>
      <h3>Chalk — styling playground</h3>
      <p>Use this area to prototype color schemes and text styles for terminal output (preview in-page).</p>
      <pre style={{background:'#081226',color:'#9be7ff',padding:8,borderRadius:6}}>Sample: nowon — Chalk color preview</pre>
    </div>
  );
}
