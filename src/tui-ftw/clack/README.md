@clack/prompts experiment (private)

Files:
- `index.tsx` — demo area for streaming/cancel UX and prompt flows (client-side mock).

What to test:
- Streaming LLM-like output, cancel (Ctrl+C) behavior, and task logs (mock generators).
