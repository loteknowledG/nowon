Figlet demo

- Live Figlet generator using the installed `figlet` npm package
- Features: font selector, live preview, **font-size control (slider + numeric)**, copy to clipboard, download `.txt`, insert into the Clack REPL

Usage:
1. Open `TUI FTW` → select `Figlet` demo.
2. Type text, choose a font, then copy/download or press "Insert into REPL" to paste the ASCII into the Console demo.
