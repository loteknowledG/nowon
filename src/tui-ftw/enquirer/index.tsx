import React from 'react';

export default function EnquirerDemo(): JSX.Element {
  return (
    <div style={{padding:12}}>
      <h3>Enquirer — prompt UX playground</h3>
      <p>Mock up Enquirer-style prompts and keybindings inside the browser for experimentation.</p>
      <small>Try: history navigation, autocomplete flows, and chained prompts (simulated).</small>
    </div>
  );
}
