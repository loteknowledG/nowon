Rezi experiment (private)

Files:
- `index.tsx` — placeholder to experiment with near-native patterns: inputs, virtual lists, global keys.

What to test:
- Global keybindings, focus traps, and deterministic replay ideas (mocked in browser)
