import React from 'react';

export default function ReziDemo(): JSX.Element {
  return (
    <div style={{padding:12}}>
      <h3>Rezi — placeholder</h3>
      <p>Prototype Rezi-style input and global keybinding UX in the browser (no native engine).</p>
      <small>Test: Tab traps, modal focus, and virtualized lists.</small>
    </div>
  );
}
